name: Build debug APK

# Builds automatically on every push, and can also be triggered manually
# from the "Actions" tab on GitHub ("Run workflow" button).
on:
  push:
    branches: [ "main" ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      # Provisions Gradle 9.5.0 itself, so the project does not need a
      # committed gradlew wrapper jar for CI builds to work.
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.5.0"

      - name: Build debug APK
        run: gradle assembleDebug --stacktrace

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: tasbihat-zahra-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
